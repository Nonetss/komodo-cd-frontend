#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Cargar el PATH del usuario si está disponible
if [ -f ~/.zshrc ]; then
  . ~/.zshrc 2>/dev/null || true
elif [ -f ~/.bashrc ]; then
  . ~/.bashrc 2>/dev/null || true
fi

bun x lint-staged